# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx1536m
# Pin the build to JDK 17. Gradle 8.7 + kapt (Kotlin 2.0, falls back to 1.9) cannot run on
# the machine-default JDK 26 (kapt fails evaluating javacOptions: "26.0.1"). Machine-specific
# path; the portable fix is a Gradle upgrade + Java toolchains, or migrating kapt -> KSP (Tier 3).
org.gradle.java.home=/Users/xavier.lagunas/Library/Java/JavaVirtualMachines/ms-17.0.19/Contents/Home
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
android.useAndroidX=true
android.enableJetifier=false
# Preserve pre-AGP8 transitive R class behavior to avoid cross-module R breakage during upgrade
android.nonTransitiveRClass=false
# AGP 8 defaults buildConfig off; keep BuildConfig generation opt-in per module (enabled where used)
android.defaults.buildfeatures.buildconfig=false
kapt.use.k2=true